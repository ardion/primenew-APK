<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jjl">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".project.AddProjectActivity"></activity>
        <activity android:name=".hiring.hiringActivity" />
        <activity android:name=".experience.experienceActivity" />
        <activity android:name=".portofolio.portofolioActivity" />
        <activity android:name=".company.formprofilecompanyActivity" />
<!--        <activity android:name=".project.AddProjectActivity" />-->
        <activity android:name=".detailworker.detailworkerActivity" />
        <activity android:name=".GithubActivity" />
        <activity
            android:name=".regis.registerLayout"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".login.login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AppIntroScreen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".helpActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".splashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>